import styled from "styled-components";

export const CartInfos = styled.span`

    width: 343px;
    margin-bottom: 22px;

    .totalPriceCart {
        font-weight: var(--font-weight-2);
        font-size: var(--font-size-5);
        color: var(--grey-50);
    }

    .totalTitle {
        font-weight: var(--font-weight-2);
        font-size: var(--font-size-5);
        color: var(--gray-100);
    }
`
